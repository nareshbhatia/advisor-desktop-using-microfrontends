import * as React from 'react';
import Typography from '@mui/material/Typography';

export function SelectClientMessage() {
  return (
    <Typography variant="h5" component="h1">
      Please select a client
    </Typography>
  );
}
